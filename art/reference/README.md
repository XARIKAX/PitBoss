# Reference art

Drop the canonical template PFP here as `template.png` (upload via GitHub web
UI: Add file -> Upload files). The style spec derived from it lives in
../README.md. Give this whole directory to the artist.
